Да. Ниже схема **1-в-1 как строить после шага 14**, чтобы были и `tickets`, и оба словаря, и потом merge. Идея ровно такая: после первого прохода отдельно берутся уникальные `root_cause`, отдельно `problem_theme`, прогоняются через OpenAI и возвращаются как mapping-словари, а потом эти словари джойнятся обратно к исходным тикетам. 

# Схема после шага 14

```text
14. Code — flatten merged object
        │
        ├────────────► 15A. Code — pass tickets
        │                  │
        │                  └────────────► 26. Merge — tickets + root_cause_dict
        │
        ├────────────► 15B. Code — prepare root_cause dictionary source
        │                  │
        │                  ▼
        │              16. Split In Batches — root_cause
        │                  │
        │                  ▼
        │              17. Code — build root_cause batch payload
        │                  │
        │                  ▼
        │              18. OpenAI — normalize root_cause
        │                  │
        │                  ▼
        │              19. Code — parse root_cause normalization
        │                  │
        │                  ▼
        │              20. Merge — collect root_cause_dict
        │
        └────────────► 21B. Code — prepare problem_theme dictionary source
                           │
                           ▼
                       22. Split In Batches — problem_theme
                           │
                           ▼
                       23. Code — build problem_theme batch payload
                           │
                           ▼
                       24. OpenAI — normalize problem_theme
                           │
                           ▼
                       25. Code — parse problem_theme normalization
                           │
                           ▼
                       27. Merge — join + problem_theme_dict

26. Merge — tickets + root_cause_dict
        │
        ▼
27. Merge — + problem_theme_dict
        │
        ▼
28. Code — flatten final enriched tickets
        │
        ├────────────► 29. Spreadsheet File — save tickets
        │
        └────────────► 30. Code — aggregate by team
                           │
                           ▼
                       31. Spreadsheet File — save team summary
```

---

# Ноды и подключения

## 14. `Code — flatten merged object`

Это твой текущий последний узел первого блока.

**Из него делай 3 линии:**

* в `15A`
* в `15B`
* в `21B`

---

## 15A. `Code — pass tickets`

Нужен только чтобы сохранить **исходный поток тикетов** для будущего merge.

Код:

```javascript
return items;
```

**Подключение:**

* `14 -> 15A`
* `15A -> 26` в **Input 1**

---

## 15B. `Code — prepare root_cause dictionary source`

Делает уникальные причины.

```javascript
function norm(v) {
  if (v === null || v === undefined) return '';
  return String(v).replace(/\s+/g, ' ').trim();
}

const seen = new Set();
const out = [];

for (const item of items) {
  const root = norm(item.json.root_cause);
  if (!root || root === 'unknown') continue;

  const key = root.toLowerCase();
  if (seen.has(key)) continue;
  seen.add(key);

  out.push({
    json: {
      original: root
    }
  });
}

return out;
```

**Подключение:**

* `14 -> 15B`
* `15B -> 16`

---

## 16. `Split In Batches — root_cause`

Настройка:

* `Batch Size = 100`

**Подключение:**

* `15B -> 16`
* `16 -> 17`

---

## 17. `Code — build root_cause batch payload`

```javascript
const causes = items.map(i => i.json.original);

return [
  {
    json: {
      causes,
      causes_as_json: JSON.stringify(causes, null, 2)
    }
  }
];
```

**Подключение:**

* `16 -> 17`
* `17 -> 18`

---

## 18. `OpenAI — normalize root_cause`

**System**

```text
Ты нормализуешь причины тикетов для аналитики.

Правила:
1. На входе список кратких формулировок причин.
2. Нужно сгруппировать похожие причины под единый канонический вариант.
3. Верни только JSON-массив.
4. Для каждой исходной причины верни:
   - original
   - canonical
   - cluster
5. canonical — короткая нормализованная формулировка.
6. cluster — более широкий класс причины.
7. Не теряй исходные записи.
```

**User**

```text
Нормализуй следующие причины:
{{$json.causes_as_json}}

Верни JSON-массив объектов вида:
{
  "original": "string",
  "canonical": "string",
  "cluster": "string"
}
```

**Подключение:**

* `17 -> 18`
* `18 -> 19`

---

## 19. `Code — parse root_cause normalization`

```javascript
function safeJsonParse(text) {
  try {
    return JSON.parse(text);
  } catch (e) {
    const match = text.match(/\[\s*{[\s\S]*}\s*\]/);
    if (match) return JSON.parse(match[0]);
    throw e;
  }
}

const raw =
  $json.message?.content ||
  $json.content ||
  $json.text ||
  $json.output_text ||
  $json.response ||
  '';

const parsed = safeJsonParse(raw);

return parsed.map(row => ({
  json: {
    root_cause_original: String(row.original || '').trim(),
    root_cause_canonical: String(row.canonical || 'unknown').trim(),
    root_cause_cluster: String(row.cluster || 'unknown').trim()
  }
}));
```

**Подключение:**

* `18 -> 19`
* `19 -> 20`

---

## 20. `Merge — collect root_cause_dict`

Этот узел нужен как **точка сбора результатов всех батчей**.

Настройка:

* `Append`

**Подключение:**

* `19 -> 20`
* `20 -> 26` в **Input 2**

---

## 21B. `Code — prepare problem_theme dictionary source`

Делает уникальные темы.

```javascript
function norm(v) {
  if (v === null || v === undefined) return '';
  return String(v).replace(/\s+/g, ' ').trim();
}

const seen = new Set();
const out = [];

for (const item of items) {
  const theme = norm(item.json.problem_theme);
  if (!theme || theme === 'unknown') continue;

  const key = theme.toLowerCase();
  if (seen.has(key)) continue;
  seen.add(key);

  out.push({
    json: {
      original: theme
    }
  });
}

return out;
```

**Подключение:**

* `14 -> 21B`
* `21B -> 22`

---

## 22. `Split In Batches — problem_theme`

Настройка:

* `Batch Size = 100`

**Подключение:**

* `21B -> 22`
* `22 -> 23`

---

## 23. `Code — build problem_theme batch payload`

```javascript
const themes = items.map(i => i.json.original);

return [
  {
    json: {
      themes,
      themes_as_json: JSON.stringify(themes, null, 2)
    }
  }
];
```

**Подключение:**

* `22 -> 23`
* `23 -> 24`

---

## 24. `OpenAI — normalize problem_theme`

**System**

```text
Ты нормализуешь темы тикетов для аналитики.

Правила:
1. На входе список кратких формулировок тем.
2. Нужно сгруппировать похожие темы под единый канонический вариант.
3. Верни только JSON-массив.
4. Для каждой исходной темы верни:
   - original
   - canonical
   - cluster
5. canonical — короткая нормализованная формулировка.
6. cluster — более широкий класс темы.
7. Не теряй исходные записи.
```

**User**

```text
Нормализуй следующие темы:
{{$json.themes_as_json}}

Верни JSON-массив объектов вида:
{
  "original": "string",
  "canonical": "string",
  "cluster": "string"
}
```

**Подключение:**

* `23 -> 24`
* `24 -> 25`

---

## 25. `Code — parse problem_theme normalization`

```javascript
function safeJsonParse(text) {
  try {
    return JSON.parse(text);
  } catch (e) {
    const match = text.match(/\[\s*{[\s\S]*}\s*\]/);
    if (match) return JSON.parse(match[0]);
    throw e;
  }
}

const raw =
  $json.message?.content ||
  $json.content ||
  $json.text ||
  $json.output_text ||
  $json.response ||
  '';

const parsed = safeJsonParse(raw);

return parsed.map(row => ({
  json: {
    problem_theme_original: String(row.original || '').trim(),
    problem_theme_canonical: String(row.canonical || 'unknown').trim(),
    problem_theme_cluster: String(row.cluster || 'unknown').trim()
  }
}));
```

**Подключение:**

* `24 -> 25`
* `25 -> 27` в **Input 2**

---

## 26. `Merge — tickets + root_cause_dict`

Тут впервые джойним словарь обратно к тикетам.

Настройки:

* `Mode: Combine`
* `Matching Fields:`

  * left: `root_cause`
  * right: `root_cause_original`

**Подключение:**

* `15A -> 26` в **Input 1**
* `20 -> 26` в **Input 2**
* `26 -> 27` в **Input 1**

---

## 27. `Merge — + problem_theme_dict`

Второй merge.

Настройки:

* `Mode: Combine`
* `Matching Fields:`

  * left: `problem_theme`
  * right: `problem_theme_original`

**Подключение:**

* `26 -> 27` в **Input 1**
* `25 -> 27` в **Input 2**
* `27 -> 28`

---

## 28. `Code — flatten final enriched tickets`

```javascript
return items.map(item => {
  const j = item.json;
  const left = j.input1 || j.source || {};
  const right = j.input2 || j.enrichment || {};

  return {
    json: {
      ...left,
      ...right
    }
  };
});
```

Если оба merge дают более сложную вложенность, используй так:

```javascript
return items.map(item => {
  const j = item.json;

  return {
    json: {
      ...(j.input1 || {}),
      ...(j.input2 || {})
    }
  };
});
```

**Подключение:**

* `27 -> 28`
* `28 -> 29`
* `28 -> 30`

---

## 29. `Spreadsheet File — save tickets`

Сохраняешь финальный тикетный датасет.

Поля будут такие:

* всё из исходного тикета
* всё из enrichment первого прохода
* `root_cause_canonical`
* `root_cause_cluster`
* `problem_theme_canonical`
* `problem_theme_cluster`

---

## 30. `Code — aggregate by team`

Это уже отдельная ветка после финального enrich. В исходнике набор командных метрик и их расчёт уже приведён готовым кодом. 

```javascript
function median(arr) {
  if (!arr.length) return null;
  const sorted = [...arr].sort((a, b) => a - b);
  const mid = Math.floor(sorted.length / 2);
  return sorted.length % 2 ? sorted[mid] : (sorted[mid - 1] + sorted[mid]) / 2;
}

const groups = {};

for (const item of items) {
  const r = item.json;
  const team = r.team || 'unknown';

  if (!groups[team]) {
    groups[team] = {
      team,
      tickets_total: 0,
      team_fault_count: 0,
      overdue_count: 0,
      rework_count: 0,
      system_defect_count: 0,
      automation_candidate_count: 0,
      durations: []
    };
  }

  const g = groups[team];
  g.tickets_total += 1;

  if (r.team_fault_flag === 'yes' || r.team_fault_flag === 'partial') g.team_fault_count += 1;
  if (r.overdue_flag === 'yes') g.overdue_count += 1;
  if (r.rework_flag === 'yes' || Number(r.rework_count || 0) > 0) g.rework_count += 1;
  if (r.request_type === 'system_defect' || r.root_cause_group === 'system') g.system_defect_count += 1;
  if (r.automation_candidate === 'yes') g.automation_candidate_count += 1;

  if (typeof r.total_duration_hours === 'number' && !isNaN(r.total_duration_hours)) {
    g.durations.push(r.total_duration_hours);
  }
}

const result = Object.values(groups).map(g => {
  const avgDuration = g.durations.length
    ? g.durations.reduce((a, b) => a + b, 0) / g.durations.length
    : null;

  const faultRate = g.tickets_total ? g.team_fault_count / g.tickets_total : 0;
  const overdueRate = g.tickets_total ? g.overdue_count / g.tickets_total : 0;
  const reworkRate = g.tickets_total ? g.rework_count / g.tickets_total : 0;
  const defectRate = g.tickets_total ? g.system_defect_count / g.tickets_total : 0;

  const normalizedDuration = avgDuration ? Math.min(avgDuration / 72, 1) : 0;

  const team_problem_score =
    0.35 * faultRate +
    0.20 * reworkRate +
    0.20 * overdueRate +
    0.15 * normalizedDuration +
    0.10 * defectRate;

  return {
    json: {
      team: g.team,
      tickets_total: g.tickets_total,
      team_fault_count: g.team_fault_count,
      team_fault_rate: Number(faultRate.toFixed(4)),
      overdue_count: g.overdue_count,
      overdue_rate: Number(overdueRate.toFixed(4)),
      rework_count: g.rework_count,
      rework_rate: Number(reworkRate.toFixed(4)),
      system_defect_count: g.system_defect_count,
      system_defect_rate: Number(defectRate.toFixed(4)),
      automation_candidate_count: g.automation_candidate_count,
      automation_candidate_rate: Number((g.automation_candidate_count / g.tickets_total).toFixed(4)),
      avg_duration_hours: avgDuration ? Number(avgDuration.toFixed(2)) : null,
      median_duration_hours: median(g.durations),
      team_problem_score: Number(team_problem_score.toFixed(4))
    }
  };
});

return result.sort((a, b) => b.json.team_problem_score - a.json.team_problem_score);
```

---

## 31. `Spreadsheet File — save team summary`

Сохраняешь агрегаты по командам.

---

# Самое короткое правило подключения

После 14 у тебя должны существовать **три параллельных потока**:

```text
A: tickets
B: root_cause_dict
C: problem_theme_dict
```

Потом:

```text
A + B -> Merge 26
(Merge 26) + C -> Merge 27
```

---

# Если хочешь совсем без ошибок в сборке

Назови ноды так:

```text
14_flatten_tickets
15A_pass_tickets
15B_root_cause_unique
16_root_cause_batches
17_root_cause_payload
18_root_cause_openai
19_root_cause_parse
20_root_cause_dict

21B_problem_theme_unique
22_problem_theme_batches
23_problem_theme_payload
24_problem_theme_openai
25_problem_theme_parse

26_merge_root_cause
27_merge_problem_theme
28_flatten_final
29_save_tickets
30_team_aggregate
31_save_team_summary
```

Если хочешь, следующим сообщением я дам тебе это уже в виде **таблицы: Node | Type | Input | Output | Код/настройка**.
