Да. Ниже даю **готовую финальную ноду `Code / Build Final Snapshot`**, которая умеет собрать **единый output contract** и для:

* `legacy/full flow`
* `matrix_only flow`
* `matrix_plus_issues flow`

То есть дальше у тебя уже будет **один стабильный JSON-контракт**, который можно:

* сохранять в Postgres
* отдавать в API
* использовать в UI
* сравнивать между запусками

---

# Куда ставить

Эту ноду ставь **в самом конце workflow**, после:

* `Code / Normalize Competency Result`
* `LLM / Build Employee Summary`
* `LLM / Build IDP`
* `LLM / One2One Employee`
* `LLM / One2One Manager`
* опционально `LLM / Jira Summary`
* опционально `LLM / Development Strategy`
* опционально `Code / Build Final Output Flags`

---

# `Code / Build Final Snapshot`

Mode: `Run Once for All Items`

```javascript
function getNodeJson(nodeName, fallback = null) {
  try {
    const nodeItems = $items(nodeName, 0, 0);
    if (nodeItems && nodeItems.length > 0) {
      return nodeItems[0].json;
    }
  } catch (e) {}
  return fallback;
}

function getNodeItems(nodeName) {
  try {
    return $items(nodeName, 0, 0).map(i => i.json);
  } catch (e) {
    return [];
  }
}

function safeArray(v) {
  return Array.isArray(v) ? v : [];
}

function nowIso() {
  return new Date().toISOString();
}

function buildOverviewProjection(snapshot) {
  return {
    employee_id: snapshot.employee.employee_id,
    generated_at: snapshot.meta.generated_at,
    full_name: snapshot.employee.full_name,
    role_id: snapshot.employee.role_id,
    grade_id: snapshot.employee.grade_id,
    overall_summary: snapshot.summary?.employee_summary?.overall_summary || '',
    strengths: safeArray(snapshot.summary?.employee_summary?.strengths),
    growth_areas: safeArray(snapshot.summary?.employee_summary?.growth_areas),
    confidence: snapshot.summary?.employee_summary?.confidence || snapshot.analysis?.confidence?.overall || 'low',
    manual_review_required: !!snapshot.analysis?.guardrails?.manual_review_required,
    career_readiness: snapshot.career?.promotion_readiness || null
  };
}

function buildCompetenciesProjection(snapshot) {
  return {
    employee_id: snapshot.employee.employee_id,
    generated_at: snapshot.meta.generated_at,
    items: safeArray(snapshot.competencies?.results)
  };
}

function buildIdpProjection(snapshot) {
  return {
    employee_id: snapshot.employee.employee_id,
    generated_at: snapshot.meta.generated_at,
    strategy: snapshot.development?.strategy || {
      focus_mode: 'stabilizing',
      primary_levers: [],
      rationale: ''
    },
    idp: safeArray(snapshot.development?.idp),
    learning_internal: safeArray(snapshot.development?.learning?.internal),
    learning_external: safeArray(snapshot.development?.learning?.external)
  };
}

function buildOne2OneProjection(snapshot) {
  return {
    employee_id: snapshot.employee.employee_id,
    generated_at: snapshot.meta.generated_at,
    employee: snapshot.one2one?.employee || {
      reflection_questions: [],
      achievements_to_highlight: [],
      growth_topics: [],
      questions_to_manager: []
    },
    manager: snapshot.one2one?.manager || {
      key_topics: [],
      risk_areas: [],
      questions_to_employee: [],
      feedback_points: [],
      support_actions: []
    }
  };
}

const root =
  getNodeJson("Code / Build Unified Root Contract") ||
  getNodeJson("Code / Build Full Jira Input") ||
  $json;

const competencyResults =
  getNodeItems("Code / Normalize Competency Result");

const employeeSummary =
  getNodeJson("LLM / Build Employee Summary", {});

const jiraSummary =
  getNodeJson("LLM / Jira Summary", null);

const idp =
  getNodeJson("LLM / Build IDP", { idp: [] });

const one2oneEmployee =
  getNodeJson("LLM / One2One Employee", {});

const one2oneManager =
  getNodeJson("LLM / One2One Manager", {});

const developmentStrategy =
  getNodeJson("LLM / Development Strategy", {
    strategy: {
      focus_mode: "stabilizing",
      primary_levers: [],
      rationale: ""
    }
  });

const mentorship =
  getNodeJson("LLM / Mentorship Suggestions", {
    mentorship: {
      mentor_as_mentee: [],
      mentor_as_mentor: [],
      role_expansion: []
    }
  });

const externalLearning =
  getNodeJson("LLM / External Learning Suggestions", {
    external_learning: []
  });

const outputFlags =
  getNodeJson("Code / Build Final Output Flags", {});

const guardrails =
  root.analysis?.guardrails ||
  getNodeJson("Code / Build Guardrail Flags", { guardrails: {} })?.guardrails ||
  {
    manual_review_required: false,
    reasons: []
  };

const coverageMap =
  root.analysis?.coverage_map ||
  getNodeJson("Code / Build Coverage Map", { coverage_map: [] })?.coverage_map ||
  [];

const confidenceSignals =
  root.analysis?.confidence?.signals ||
  getNodeJson("Code / Compute Confidence Signals", { confidence_signals: {} })?.confidence_signals ||
  {};

const outliers =
  root.analysis?.outliers ||
  getNodeJson("Code / Detect Outliers", { outliers: [] })?.outliers ||
  [];

const gradeGap =
  root.career?.grade_gap ||
  getNodeJson("Code / Compute Grade Gap", { grade_gap: [] })?.grade_gap ||
  [];

const promotionReadiness =
  root.career?.promotion_readiness ||
  getNodeJson("Code / Compute Promotion Heuristics", { promotion_readiness: {} })?.promotion_readiness ||
  {
    current_grade_fit: "partial",
    next_grade: "",
    readiness: "not_ready",
    blockers: []
  };

const internalLearning =
  root.learning_catalog?.assets
    ? []
    : (
        getNodeJson("Code / Match Internal Trainings", { learning_recommendations: [] })?.learning_recommendations ||
        []
      );

const assessmentMode =
  outputFlags.assessment_mode ||
  root.workflow_config?.assessment_mode ||
  "full";

const performanceInferenceAllowed =
  outputFlags.performance_inference_allowed !== undefined
    ? outputFlags.performance_inference_allowed
    : !!root.workflow_config?.performance_inference_allowed;

const developmentInferenceAllowed =
  outputFlags.development_inference_allowed !== undefined
    ? outputFlags.development_inference_allowed
    : !!root.workflow_config?.development_inference_allowed;

const dataCompleteness =
  outputFlags.data_completeness ||
  ((safeArray(root.employee_issues).length > 0 || safeArray(root.issues).length > 0) ? "medium" : "low");

const employee = root.employee || {
  employee_id: null,
  full_name: null,
  email: null,
  role_id: null,
  grade_id: null
};

const metaGeneratedAt = nowIso();
const assessmentId =
  root.meta?.assessment_id ||
  root.assessment_id ||
  metaGeneratedAt.replace(/[-:.TZ]/g, '').slice(0, 14) + "_" + (employee.employee_id || "unknown");

const snapshot = {
  meta: {
    version: "v2",
    assessment_id: assessmentId,
    generated_at: metaGeneratedAt,
    status: "completed",
    source: root.scoring_input?.source || "workflow",
    lookback_days: root.workflow_config?.lookback_days || 180,
    snapshot_hash: root.meta?.snapshot_hash || "",
    source_max_updated_at: root.meta?.source_max_updated_at || null
  },

  employee: {
    employee_id: employee.employee_id || null,
    full_name: employee.full_name || null,
    email: employee.email || null,
    role_id: employee.role_id || null,
    grade_id: employee.grade_id || null,
    team_id: employee.team || employee.team_id || null,
    manager_id: employee.manager_id || null,
    login: employee.login || null,
    position: employee.position || null
  },

  inputs: {
    workflow_config: root.workflow_config || {},
    self_assessments: safeArray(root.assessment_input?.self_assessments),
    competency_catalog_version: root.competency_catalog?.version || root.meta?.competency_catalog_version || null,
    career_track_version: root.career_tracks?.version || root.meta?.career_track_version || null,
    training_catalog_version: root.learning_catalog?.version || root.meta?.training_catalog_version || null
  },

  raw_data: {
    jira_user: root.jira_user || null,
    issues_count: safeArray(root.issues || root.employee_issues).length,
    issues_updated_max: root.meta?.issues_updated_max || null
  },

  normalized: {
    issues: safeArray(root.normalized?.issues || root.issues || root.employee_issues),
    evidence_trace: safeArray(root.normalized?.evidence_trace),
    synthetic_evidence: safeArray(root.normalized?.synthetic_evidence)
  },

  analysis: {
    assessment_mode: assessmentMode,
    data_completeness: dataCompleteness,
    performance_inference_allowed: performanceInferenceAllowed,
    development_inference_allowed: developmentInferenceAllowed,

    coverage_map: coverageMap,

    confidence: {
      overall: employeeSummary?.confidence || "low",
      signals: confidenceSignals
    },

    guardrails: {
      manual_review_required: !!guardrails.manual_review_required,
      reasons: safeArray(guardrails.reasons)
    },

    skill_gaps: safeArray(root.analysis?.skill_gaps),
    outliers: safeArray(outliers)
  },

  competencies: {
    results: competencyResults
  },

  career: {
    grade_gap: safeArray(gradeGap),
    promotion_readiness: {
      current_grade_fit: promotionReadiness.current_grade_fit || "partial",
      next_grade: promotionReadiness.next_grade || "",
      readiness: promotionReadiness.readiness || "not_ready",
      blockers: safeArray(promotionReadiness.blockers)
    }
  },

  development: {
    strategy: developmentStrategy.strategy || {
      focus_mode: "stabilizing",
      primary_levers: [],
      rationale: ""
    },

    idp: safeArray(idp.idp),

    learning: {
      internal: safeArray(internalLearning),
      external: safeArray(
        externalLearning.external_learning ||
        externalLearning.resources ||
        []
      )
    },

    mentorship: mentorship.mentorship || {
      mentor_as_mentee: [],
      mentor_as_mentor: [],
      role_expansion: []
    }
  },

  one2one: {
    employee: {
      reflection_questions: safeArray(one2oneEmployee.reflection_questions),
      achievements_to_highlight: safeArray(one2oneEmployee.achievements_to_highlight),
      growth_topics: safeArray(one2oneEmployee.growth_topics),
      questions_to_manager: safeArray(one2oneEmployee.questions_to_manager)
    },
    manager: {
      key_topics: safeArray(one2oneManager.key_topics),
      risk_areas: safeArray(one2oneManager.risk_areas),
      questions_to_employee: safeArray(one2oneManager.questions_to_employee),
      feedback_points: safeArray(one2oneManager.feedback_points),
      support_actions: safeArray(one2oneManager.support_actions)
    }
  },

  summary: {
    employee_summary: {
      overall_summary: employeeSummary.overall_summary || "",
      strengths: safeArray(employeeSummary.strengths),
      growth_areas: safeArray(employeeSummary.growth_areas),
      alignment: employeeSummary.alignment || {
        overestimated: [],
        underestimated: [],
        aligned: []
      },
      top_priorities_next_6m: safeArray(employeeSummary.top_priorities_next_6m),
      confidence: employeeSummary.confidence || "low"
    },

    jira_summary: jiraSummary
      ? {
          overall_summary: jiraSummary.overall_summary || "",
          activity_level: jiraSummary.activity_level || "low",
          delivery_assessment: jiraSummary.delivery_assessment || "",
          engineering_signal: jiraSummary.engineering_signal || "",
          risks: safeArray(jiraSummary.risks),
          confidence: jiraSummary.confidence || "low"
        }
      : {
          overall_summary: assessmentMode === "matrix_only"
            ? "Jira-based activity summary is not available in matrix-only mode."
            : "",
          activity_level: "low",
          delivery_assessment: "",
          engineering_signal: "",
          risks: assessmentMode === "matrix_only"
            ? ["No Jira/activity evidence available for delivery summary"]
            : [],
          confidence: "low"
        }
  }
};

const overview = buildOverviewProjection(snapshot);
const competenciesProjection = buildCompetenciesProjection(snapshot);
const idpProjection = buildIdpProjection(snapshot);
const one2oneProjection = buildOne2OneProjection(snapshot);

return [{
  json: {
    snapshot,
    projections: {
      overview,
      competencies: competenciesProjection,
      idp: idpProjection,
      one2one: one2oneProjection
    }
  }
}];
```

---

# Что делает эта нода

Она:

* берет root contract из:

  * `Code / Build Unified Root Contract`
  * или `Code / Build Full Jira Input`
* подтягивает результаты из:

  * `Code / Normalize Competency Result`
  * `LLM / Build Employee Summary`
  * `LLM / Jira Summary`
  * `LLM / Build IDP`
  * `LLM / One2One Employee`
  * `LLM / One2One Manager`
  * опционально `LLM / Development Strategy`
* собирает **единый snapshot**
* сразу строит **projections**:

  * `overview`
  * `competencies`
  * `idp`
  * `one2one`

То есть это уже почти готовый объект для:

* `assessment_snapshots.payload`
* `latest_assessment_views.overview`
* `latest_assessment_views.competencies`
* `latest_assessment_views.idp`
* `latest_assessment_views.one2one`

---

# Если у тебя названия нод другие

Внутри кода есть жесткие ссылки вида:

```javascript
getNodeJson("LLM / Build Employee Summary")
```

Если у тебя ноды названы иначе, просто замени строки:

* `"Code / Build Unified Root Contract"`
* `"Code / Build Full Jira Input"`
* `"Code / Normalize Competency Result"`
* `"LLM / Build Employee Summary"`
* `"LLM / Jira Summary"`
* `"LLM / Build IDP"`
* `"LLM / One2One Employee"`
* `"LLM / One2One Manager"`
* `"LLM / Development Strategy"`
* `"LLM / Mentorship Suggestions"`
* `"LLM / External Learning Suggestions"`
* `"Code / Build Final Output Flags"`
* `"Code / Build Guardrail Flags"`
* `"Code / Build Coverage Map"`
* `"Code / Compute Confidence Signals"`
* `"Code / Detect Outliers"`
* `"Code / Compute Grade Gap"`
* `"Code / Compute Promotion Heuristics"`
* `"Code / Match Internal Trainings"`

---

# Что я бы еще рекомендовал рядом

## 1. Сразу после этой ноды сделать 4 маленьких projection-ноды

Если хочешь писать в Postgres раздельно:

### `Code / Extract Overview Projection`

```javascript
return [{ json: $json.projections.overview }];
```

### `Code / Extract Competencies Projection`

```javascript
return [{ json: $json.projections.competencies }];
```

### `Code / Extract IDP Projection`

```javascript
return [{ json: $json.projections.idp }];
```

### `Code / Extract One2One Projection`

```javascript
return [{ json: $json.projections.one2one }];
```

---

## 2. Сразу добавить snapshot status flags в UI

Потому что для `matrix_only` это критично.

На UI желательно показывать:

* `assessment_mode`
* `data_completeness`
* `performance_inference_allowed`
* `development_inference_allowed`

Они уже лежат в:

```json
snapshot.analysis
```

---

## 3. Для `matrix_only` не показывать Jira Summary как полноценный perf summary

В этой версии я уже сделал безопасный fallback:

* если Jira Summary нет
* и режим `matrix_only`
* будет явный текст, что Jira summary недоступен

---

# Как будет выглядеть выход

На выходе ноды ты получишь:

```json
{
  "snapshot": { ...полный контракт... },
  "projections": {
    "overview": { ... },
    "competencies": { ... },
    "idp": { ... },
    "one2one": { ... }
  }
}
```

---

# Самый практичный следующий шаг

Теперь тебе логично сделать одно из двух:

**вариант 1:** я соберу тебе **ноды записи этого snapshot и projections в Postgres для n8n**
**вариант 2:** я соберу тебе **пакет безопасных LLM prompts целиком под matrix_only / mixed / full режимы**, чтобы output не галлюцинировал
